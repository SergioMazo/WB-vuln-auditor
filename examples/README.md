# Examples

The files in this directory are synthetic and safe to publish.

- [`example-output.csv`](example-output.csv) shows the exact CSV schema emitted
  for one confirmed result.

The address belongs to a documentation-only range. It is not a target and must
not be replaced with real assessment data in version control.
